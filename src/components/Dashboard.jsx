import React, { useContest } from "react";
import { AuthContext } from "./Auth";
// import { Redirect } from "react-router-dom";

const Dashboard = () => {
    // const { currentUser } = useContext(AuthContext);
    // if (!currentUser) {
    //     return <Redirect to="/login" />;
    // }
    // return (
    //     <div>
    //         <h1>Dashboard</h1>
    //     </div>
    // );
};

export default Dashboard;